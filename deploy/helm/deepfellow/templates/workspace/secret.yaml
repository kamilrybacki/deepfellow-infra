{{- if .Values.workspace.enabled }}
{{- $cred := include "deepfellow.credentials" . | fromYaml }}
{{- $w := .Values.workspace }}
{{- $secretName := include "deepfellow.credSecretName" (dict "context" . "component" "workspace") }}
{{/* --- workspace-owned credentials --- */}}
{{- $sec := include "deepfellow.chartSecret" (dict "context" . "secretName" $secretName "creds" (list $cred.workspace.auth.sessionSecret $cred.workspace.auth.bootstrapAdmin.password)) }}
{{- with $sec }}
{{ . }}
{{- end }}
{{- end }}
