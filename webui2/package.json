{
  "name": "deepfellow-infra-webui2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -R node_modules/@simplito/deepfellow-dashboard-single-instance-dfinfra/dist/. dist/",
    "buildx": "npm run build && cp -r dist/* ../static/",
    "dashboard": "deepfellow-dashboard-dfinfra"
  },
  "devDependencies": {
    "@simplito/deepfellow-dashboard-single-instance-dfinfra": "1.0.136"
  }
}
